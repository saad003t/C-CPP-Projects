## C-CPP-Projects

A collection of small C/C++ console-based mini-projects — built as practice exercises and coursework. Each file is a standalone program that runs in the terminal.

## Projects

File --- Description
e-commercePROJECT.cpp	--- Console-based e-commerce simulation (product listing, cart/checkout-style logic).
loginPage.cpp ---	Console-based login system.
toDoList.cpp	--- Console-based to-do list manager.


## How to run

Each file is self-contained. Compile and run with g++ (or any C/C++ compiler):


## Structure

This repo will keep growing with more small C/C++ mini-projects over time — each new program added as its own file, with an entry added to the table above.


## Requirements

A C++ compiler (e.g. g++)
No external libraries required, unless noted in a specific file.
